version: "3.9"

services:
  app:
    image: ghcr.io/<github-username>/english-learning-app:latest
    container_name: english-learning-app
    restart: always
    ports:
      - "80:3000"
      - "3001:3001"
    environment:
      NODE_ENV: production
